# if you want to print the valuesof pi
import math
print("the value of pi is ",math.pi)    # the value of pi

import statistics
x = [150,250,350,450]

print("the mean of the values of x is",statistics.mean(x)) # the mean of the values of x
